# Incoming visual assets

Temporary staging folder for approved visual assets before they are moved into the public website asset structure.

Expected source package: `kb_visual_assets.zip`

Claude Code should:

1. extract or copy the approved image files into this folder;
2. move final web-ready assets into `content/en/assets/images/approved/`;
3. update the relevant placeholder mappings and captions;
4. remove temporary files from `_incoming/` once the integration is complete.

Do not place confidential Musashi material in this folder.
